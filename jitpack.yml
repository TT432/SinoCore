jdk:
  - openjdk17
install:
  - "ls $SDKMAN_DIR"
  - "ls $SDKMAN_DIR/openjdk17"
  - "ls $SDKMAN_DIR/java"
  - "ls $SDKMAN_DIR/jdk"
  - "ls $SDKMAN_DIR/jdk17"
# before_install:
#   - "chmod 777 gradlew"
# install:
#   - "./gradlew build assemble publishToMavenLocal --refresh-dependencies"